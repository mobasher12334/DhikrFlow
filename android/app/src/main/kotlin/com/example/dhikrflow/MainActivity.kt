package com.example.dhikrflow

import io.flutter.embedding.android.FlutterActivity

/**
 * Main entry point for DhikrFlow on Android.
 *
 * Uses the modern Flutter v2 embedding — no manual plugin registration
 * is required; Flutter's auto-detection mechanism handles it.
 */
class MainActivity : FlutterActivity()
